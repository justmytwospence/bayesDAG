{
  "name": "bayesdag",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "comments": {
    "PACKAGE_VERSION": "Must match the mathjax-full version below. The define kills MathJax's eval(\"require\") Node-detection branch so the bundle runs in bare V8; a stale value re-enables it and math rendering breaks at runtime. tests/test_rollback_and_export.py keeps this file's version in sync with pyproject.toml.",
    "dependencies": "elkjs and mathjax-full are BUNDLED into the wheel — see THIRD_PARTY_LICENSES. Nothing else ships: js/index.js imports nothing at runtime."
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm run build:widget && npm run build:mathjax && npm run build:elk && npm run build:css",
    "build:widget": "esbuild js/index.js --minify --format=esm --bundle --outfile=src/bayesdag/static/widget.js",
    "build:mathjax": "esbuild js/mathjax_entry.js --bundle --format=iife --platform=browser --define:PACKAGE_VERSION='\"3.2.1\"' --outfile=src/bayesdag/static/mathjax.bundle.js",
    "build:elk": "node -e \"['elk-api.js','elk-worker.min.js'].forEach(f=>require('fs').copyFileSync('node_modules/elkjs/lib/'+f,'src/bayesdag/static/'+f))\"",
    "build:css": "node -e \"require('fs').copyFileSync('js/widget.css','src/bayesdag/static/widget.css')\"",
    "dev": "esbuild js/index.js --sourcemap=inline --format=esm --bundle --outfile=src/bayesdag/static/widget.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  },
  "dependencies": {
    "elkjs": "^0.9.3",
    "mathjax-full": "^3.2.1"
  }
}
